<%= form_with(model: poke_event, local: true) do |form| %>
  <% if poke_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poke_event.errors.count, "error") %> prohibited this poke_event from being saved:</h2>

      <ul>
      <% poke_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.hidden :user_id %>
  </div>

  <div class="field">
    <%= form.label :land_mark_id %>
    <%= form.text_field :land_mark_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
